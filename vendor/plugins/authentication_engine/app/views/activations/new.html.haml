- title "Por favor, termina tú registro"
.register
  .grid_12
    .info-block
      %p
        %strong Es importante que rellenes todos los datos
    .info-block
      %p
        La información recogida es útil para datos estadísticos, de esta forma
        podremos tener información relevante de cómo estamos distribuidos geográficamente
        y poder organizarnos de forma más efectiva
    .divider-long

  .grid_21
    -form_for @user, :url => activate_path(:id => @user.id), :html => { :method => :post } do |f|
      =f.error_messages
      -if @invitation
        =f.hidden_field :invitation_id
        =hidden_field_tag :invitation_token, @invitation.token
        %fieldset
          %p
            =f.label :name, "Nombre"
            =f.text_field :name
            =obligatorio
          %p
            =f.label :email, "Email"
            =f.text_field :email
            =obligatorio
      
              
      %p.submit.grid_13=f.submit "Enviar"