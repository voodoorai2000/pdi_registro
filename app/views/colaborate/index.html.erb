<%= error_messages_for :user %>

<% form_for @user, :url => account_path do |f| -%>
  <%= render :partial => "shared/areas", :locals => { :f => f, :object => @user } %>
  <%= hidden_field :colaborar, :true %>
  
  <p class="submit">
    <%= f.submit "Enviar" %>
  </p>
<% end -%>